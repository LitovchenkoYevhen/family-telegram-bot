version: '3.8'

services:
  bot:
    build: .
    container_name: family-telegram-bot
    restart: always
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/family_finances.db 